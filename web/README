To setup the python virtual environment pipenv has been used.
pipenv replace the usage of virtualenvs and pip dependencies (requirements.txt file).
Through pip, install pipenv:
	$> pip install --user pipenv

Then, move into the web folder and run:
	$> pipenv install

All the required packages and dependencies, specified in Pipfile, will be installed.
